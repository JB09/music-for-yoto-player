services:
  yoto-scraper:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - ./downloads:/app/downloads
    environment:
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY}
      - YOTO_DEVELOPER_API=${YOTO_DEVELOPER_API:-}
      - FLASK_SECRET_KEY=${FLASK_SECRET_KEY:-}
      - OUTPUT_DIR=/app/downloads
