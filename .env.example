# Required for AI Chat mode
ANTHROPIC_API_KEY=sk-ant-...

# Optional: Yoto Developer API client ID for MYO card upload
YOTO_CLIENT_ID=

# Optional: Public base URL when behind a reverse proxy or custom domain
# Example: https://yoto.example.com
YOTO_REDIRECT_URI=

# Optional: Flask secret key (auto-generated if not set)
FLASK_SECRET_KEY=

# ── Music Provider ─────────────────────────────────────────────────
# Choose your music source: "youtube" (default) or "plex"
MUSIC_PROVIDER=youtube

# ── YouTube Provider Settings ──────────────────────────────────────
# URL of the yt-dlp-host sidecar service (see docker-compose.yml)
DOWNLOAD_SERVICE_URL=http://ytdlp:5000
# API key for the yt-dlp-host service (optional, depends on yt-dlp-host config)
DOWNLOAD_API_KEY=

# ── Plex Provider Settings (only if MUSIC_PROVIDER=plex) ──────────
# Your Plex server URL (e.g. http://192.168.1.100:32400)
PLEX_URL=
# Your Plex authentication token
PLEX_TOKEN=
# Name of the music library section in Plex (default: "Music")
PLEX_MUSIC_LIBRARY=Music
